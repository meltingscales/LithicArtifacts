[project]
name = "lithicartifacts"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "pyxel>=2.9.6",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.0",
]
